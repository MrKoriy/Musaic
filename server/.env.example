# Musaic Local Server Configuration

PORT=3001

# SQLite database path (default: ./musaic.db)
DB_PATH=./musaic.db

# Where downloaded VK/SC tracks are saved
DOWNLOADS_DIR=./downloads

# Local music library (FLAC files) - used by LocalFLACProvider
MUSIC_DIR=./music
